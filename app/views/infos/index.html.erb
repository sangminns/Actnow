<br><br><br><br><br>

<%= link_to '새 글쓰기' , infos_info_new_path  %><br>
<%= link_to 'Home', '/' %>

<h2>현재 추가한 데이터 리스트</h2>
<% @storeInfo.each do |i| %>
    이름 : <%= i.infoTitle %><br>
    종목 : <%= i.game %><br>
    지역 : <%= i.region %><br>
    주소 : <%= i.address %><br>
    위도 : <%= i.location_lat %><br>
    경도 : <%= i.location_lng %><br>
    사진 : <%= image_tag i.info_image_url.thumb1.url %><br>
    
    <% i.info_attachments.each do |p| %>
      <%= image_tag p.upcast.thumb1.url %>
      <br><br>
    <% end %>
    
    <a href="/destroy/<%= i.id %>">[삭제]</a> |
    <%= link_to '[수정]', edit_info_path(i.id) %><hr>
    
<% end %>
