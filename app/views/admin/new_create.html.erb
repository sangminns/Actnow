<h2>관리자 페이지 - 데이터 추가</h2>
<a href="/">Home으로 돌아가기</a><hr>

<%= render 'form' %>

<hr>

<h2>현재 추가한 데이터 리스트</h2>
<% @storeInfo.each do |i| %>
    이름 : <%= i.infoTitle %><br>
    종목 : <%= i.game %><br>
    지역 : <%= i.region %><br>
    주소 : <%= i.address %><br>
    위도 : <%= i.location_lat %><br>
    경도 : <%= i.location_lng %><br>
    사진 : <%= image_tag i.info_image_url.thumb1.url %><br>
    
    <% i.info_attachments.each do |p| %>
      <%= image_tag p.upcast.thumb1.url %>
      <br><br>
    <% end %>
    
    <a href="/destroy/<%= i.id %>">[삭제]</a><hr> 
    <%= link_to 'Edit', edit_info_path(i.id) %> |
<% end %>
